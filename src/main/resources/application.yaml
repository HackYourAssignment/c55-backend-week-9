spring:
  application:
    name: hyf_shop
  datasource:
    url: '${DB_URL}'
    username: '${DB_USERNAME}'
    password: '${DB_PASSWORD}'
server:
  port: '8080'
sql:
  init:
    # runs schema.sql automatically every time the app starts
    mode: always